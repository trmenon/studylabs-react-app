import { 
    setCookie,
    removeCookie, 
    getCookie 
} from './cookie';

import {
    setLocalStorage,
    removeLocalStorage,
    getLocalStorageByKey
} from './localStorage';

export {
    setCookie,
    removeCookie, 
    getCookie,
    setLocalStorage,
    removeLocalStorage,
    getLocalStorageByKey 
};